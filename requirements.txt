# Florian Dheer Pipeline - Python dependencies
# Single source of truth. install.py and install_dependencies.py both read this.
#
# Install with:
#   pip install -r requirements.txt
# or run the friendly bootstrap:
#   python install.py

# --- Core (always required) ---
pillow>=10.0.0            # Image processing and logo display

# --- Image metadata ---
pyexiv2>=2.8.0            # EXIF metadata read/write for image scripts

# --- Invoice pipeline ---
setuptools<81             # invoice2data needs pkg_resources (removed in Python 3.13+ stdlib)
pdfplumber>=0.9.0         # PDF text extraction for invoice processing
invoice2data>=0.4.0       # Template-based invoice data extraction
